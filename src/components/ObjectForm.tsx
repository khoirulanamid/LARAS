import React from "react";

export type UIObject = {
  id: string;
  name: string;
  note?: string;
};

export default function ObjectForm() {
  return <div className="hidden">{/* Placeholder objek pendukung */}</div>;
}
